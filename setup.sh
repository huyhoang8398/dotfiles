#!/bin/bash

mv  Pictures ~
mv .zshrc ~
mv .zshenv ~
mv .xprofile ~
mv nvim ~/.config
mv i3 ~/.config
sudo mv xorg.conf.d /etc/X11
mv picom ~/.config
mv mimeapps.list ~/.config
mv dunst ~/.config
mv i3blocks ~/.config
mv rofi ~/.config
mv .themes ~
mv icons ~/.local/share
mv .icons ~
sudo mv fonts /usr/share/fonts

